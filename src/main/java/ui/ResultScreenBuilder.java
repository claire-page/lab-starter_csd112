package ui;

import javafx.scene.Parent;
import javafx.util.Builder;

public class ResultScreenBuilder implements Builder<Parent> {

    //this will initialize the screen. Note that I only want it available when game is done.

    @Override
    public Parent build() {
        return null;
    }
}
